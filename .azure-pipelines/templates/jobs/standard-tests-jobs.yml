jobs:
  - job: test
    variables:
      PYTHON_VERSION: 3.9
    strategy:
      matrix:
        macos-py27:
          IMAGE_NAME: macOS-10.15
          PYTHON_VERSION: 2.7
          TOXENV: py27
    pool:
      vmImage: $(IMAGE_NAME)
    steps:
      - template: ../steps/tox-steps.yml
