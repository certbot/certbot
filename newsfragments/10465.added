A new command line flag, --ip-address, has been added. This requests
certificates with IP address SANs. Note that for Let's Encrypt's implementation
of IP address certificates, you'll also need to pass
`--preferred-profile shortlived`.
